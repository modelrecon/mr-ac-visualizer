streamlit app to visualize the data created in activity cube.

Heatmap of token × layer activity
Line charts of token trace across layers
Explorer for attention maps (if there in cube data)


just run by streamlit run app.py
